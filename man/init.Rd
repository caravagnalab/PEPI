% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Inits.R
\name{init}
\alias{init}
\title{Initialize a PEPI object}
\usage{
init(clade_statistics, counts, genomic_constants)
}
\arguments{
\item{clade_statistics}{Tibble containing clade-level mutation and VAF information.}

\item{counts}{Tibble containing population counts over time.}

\item{genomic_constants}{Tibble with columns `variable` and `value`,
including at least `genome_length` and `mu`.}
}
\value{
An object of class \code{"PEPI"}.
}
\description{
This function initializes a PEPI object containing clade-level VAF statistics,
population counts across time points, and genomic constants used by the model.
}
\examples{
library(tibble)

clade_statistics <- tibble(
  cluster_name = "S",
  n_muts = 400,
  vaf_1_n = 0,
  vaf_1_p = 0.25,
  vaf_2_n = 0,
  vaf_2_p = 0.28,
  is_clade = TRUE,
  names = "first clade",
  driver_type = "",
  has_driver = FALSE,
  rewind = FALSE
)

counts <- tibble(
  count_n = c(1e5, 1.2e5),
  count_p = c(7e4, 9e4),
  time = c(5, 6)
)

genomic_constants <- tibble(
  variable = c("genome_length", "mu"),
  value = c(3e9, 1e-7)
)

pepi <- init(clade_statistics, counts, genomic_constants)
}
