% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitters.R
\name{fit_multirates}
\alias{fit_multirates}
\title{Fit the unified multirates model.}
\usage{
fit_multirates(
  x,
  cmdstan_path = cmdstanr::cmdstan_path(),
  method = c("variational", "sample"),
  ndraws = 1000,
  chains = 4,
  seed = 45,
  init = NULL,
  mu = 1e-07,
  l = 2.7e+09,
  t_min = 0,
  ms_epi = 0,
  sigma_epi = 0.5,
  alpha_lambda = 1,
  beta_lambda = 1,
  alpha_n_wt = 1,
  beta_n_wt = 10,
  alpha_p_wt = 1,
  beta_p_wt = 10,
  min_kappa = 10,
  max_kappa = 1000,
  min_sigma_count = 0.01,
  max_sigma_count = 1,
  include_poisson = 1L,
  include_trunk = 1L
)
}
\arguments{
\item{x}{PEPI_Multirates object.}

\item{cmdstan_path}{String specifying the path to the cmdstan installation.}

\item{method}{"variational" (default, fast approximate posterior) or "sample" (full MCMC).}

\item{ndraws}{Number of posterior draws (variational: output_samples; sample: iter_sampling).}

\item{chains}{Number of MCMC chains (only used when method = "sample").}

\item{seed}{Seed of the computation.}

\item{init}{List of initialization values, or NULL to use a built-in
default (see \code{.multirates_default_init()}).}

\item{mu}{Mutation rate per division per bp per allele.}

\item{l}{Length of the genome.}

\item{t_min}{Lower bound for the tmrca prior.}

\item{ms_epi, sigma_epi}{Lognormal prior hyperparameters for s_epi.}

\item{alpha_lambda, beta_lambda}{Gamma prior hyperparameters for lambda_n.}

\item{alpha_n_wt, beta_n_wt, alpha_p_wt, beta_p_wt}{Gamma prior hyperparameters
for the wt switch rates omega_n_wt/omega_p_wt.}

\item{min_kappa, max_kappa}{Uniform prior bounds for kappa.}

\item{min_sigma_count, max_sigma_count}{Uniform prior bounds for sigma_count.}

\item{include_poisson, include_trunk}{Stan model switches (0/1).}
}
\value{
PEPI_Multirates object with `inference$multirates`, `stan_data$multirates`
  and `index_maps` populated.
}
\description{
Compiles and runs \code{inst/multirates_positive_s.stan} on a
\code{PEPI_Multirates} object built with \code{init_multirates()}.
}
\examples{
\donttest{
sim = simulate_multirates_tree(
  sampling_times = c(3, 6, 9), tmrca = 1, t_min = 0,
  lambda_n = 1, s_epi = 0.15, omega_n_wt = 5e-3, omega_p_wt = 2e-3,
  drivers = list(list(id = "KRAS", t_driver = 2, s_driver = 0.3,
      omega_n_driver = 5e-3, omega_p_driver = 2e-3,
      ms_driver = -0.5, sigma_driver = 0.5,
      alpha_n_driver = 1, beta_n_driver = 10, alpha_p_driver = 1, beta_p_driver = 10)),
  clades_wt = list(list(id = "c1", t_clade_wt = 1.5)),
  mu = 1e-7, l = 2.7e9, kappa = 20, sigma_count = 0.1, seed = 42
)
x = init_multirates(sim$tables, m_trunk = sim$truth$m_trunk)
x = fit_multirates(x, cmdstan_path = cmdstanr::cmdstan_path(),
  method = "variational", ndraws = 500, seed = 45,
  mu = 1e-7, l = 2.7e9, t_min = 0, ms_epi = 0, sigma_epi = 0.5,
  alpha_lambda = 1, beta_lambda = 1, alpha_n_wt = 1, beta_n_wt = 10,
  alpha_p_wt = 1, beta_p_wt = 10, min_kappa = 5, max_kappa = 200,
  min_sigma_count = 0.01, max_sigma_count = 1)
}
}
