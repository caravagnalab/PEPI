% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitters.R
\name{.multirates_default_init}
\alias{.multirates_default_init}
\title{Build a default set of initial values for the multirates model.}
\usage{
.multirates_default_init(data)
}
\arguments{
\item{data}{Stan data list, as built by \code{build_stan_data_multirates()}.}
}
\value{
A list (of length 1) suitable for cmdstanr's `init` argument.
}
\description{
Several time parameters have bounds that depend on the *sampled* value of
`tmrca` (e.g. `t_clade_wt` is bounded above by the earliest sampling time,
below by `tmrca`), so Stan's default random initialization frequently
draws an infeasible `tmrca` and fails before sampling starts. This builds
a single feasible starting point instead.
}
\keyword{internal}
