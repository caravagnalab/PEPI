% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multirates_generative_model.R
\name{simulate_multirates_tree}
\alias{simulate_multirates_tree}
\title{Simulate synthetic input tables for `init_multirates()`.}
\usage{
simulate_multirates_tree(
  sampling_times,
  tmrca,
  t_min,
  lambda_n,
  s_epi,
  omega_n_wt,
  omega_p_wt,
  drivers = list(),
  driver_n = list(),
  driver_p = list(),
  dc = list(),
  clades_wt = list(),
  mu = 1e-07,
  l = 2.7e+09,
  kappa = 200,
  sigma_count = 0.1,
  seed = NULL
)
}
\arguments{
\item{sampling_times}{Numeric vector of sampling times.}

\item{tmrca}{Time of most recent common ancestor.}

\item{t_min}{Time origin (lower bound of the tmrca prior / start of the wt lineage).}

\item{lambda_n}{Growth rate of "-" cells.}

\item{s_epi}{Fitness of "+" vs "-" wt cells.}

\item{omega_n_wt, omega_p_wt}{wt switch rates.}

\item{drivers}{List of driver specs, each a list with `id`, `t_driver`, `s_driver`,
`omega_n_driver`, `omega_p_driver`, `ms_driver`, `sigma_driver`,
`alpha_n_driver`, `beta_n_driver`, `alpha_p_driver`, `beta_p_driver`.}

\item{driver_n}{List of driver-only "-"-confined specs: `id`, `t_driver_n`,
`s_driver_n`, `ms_driver_n`, `sigma_driver_n`.}

\item{driver_p}{List of driver-only "+"-confined specs: `id`, `t_driver_p`,
`s_driver_p`, `ms_driver_p`, `sigma_driver_p`.}

\item{dc}{List of driver+clade combo specs: `id`, `delta_t1` (tmrca to driver
acquisition), `delta_t2` (driver acquisition to clade/switch event), `s_dc`,
`omega_n_dc`, `omega_p_dc`, `ms_dc`, `sigma_dc`, `alpha_n_dc`, `beta_n_dc`,
`alpha_p_dc`, `beta_p_dc`.}

\item{clades_wt}{List of wt-background clade specs: `id`, `t_clade_wt`.}

\item{mu}{Mutation rate per division per bp per allele.}

\item{l}{Length of the genome.}

\item{kappa}{Concentration parameter for the beta_proportion CCF/fraction noise.}

\item{sigma_count}{Lognormal sd for population-size noise.}

\item{seed}{Optional RNG seed.}
}
\value{
A list with `tables` (ready for `init_multirates()`) and `truth`
  (the ground-truth parameter values used to simulate the data).
}
\description{
Simulate synthetic input tables for `init_multirates()`.
}
\examples{
simulate_multirates_tree(sampling_times = c(2,5,8), tmrca = 1, t_min = 0,
  lambda_n = 1, s_epi = 0.2, omega_n_wt = 1e-3, omega_p_wt = 1e-4,
  drivers = list(list(id = "KRAS", t_driver = 3, s_driver = 0.3,
    omega_n_driver = 1e-3, omega_p_driver = 1e-4, ms_driver = -0.5,
    sigma_driver = 0.5, alpha_n_driver = 1, beta_n_driver = 10,
    alpha_p_driver = 1, beta_p_driver = 10)),
  seed = 1908)
}
